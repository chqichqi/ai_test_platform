# 功能测试SKILL说明文档

## 文件位置
`功能测试SKILL示例.json`

## 整体结构

### 1. 基本信息（Base Info）
```json
{
  "name": "Web端用户管理功能测试专家",
  "code": "functional_user_management_v1",
  "skill_type": "functional",
  "description": "...",
  "tags": ["用户管理", "Web测试", "功能测试", "表单验证"],
  "is_global": true,
  "is_default": false
}
```
**作用**：定义SKILL的基本属性和分类

---

### 2. 角色定义（Role）
```json
"role": {
  "name": "用户管理功能测试专家",
  "description": "你是一位资深的用户管理系统功能测试专家...",
  "expertise": [...],
  "behavior_rules": [...]
}
```
**作用**：
- 告诉AI它应该扮演什么角色
- 明确它的专业能力和行为准则
- 影响生成测试用例的风格和质量

---

### 3. 输入定义（Input）
```json
"input": {
  "required_fields": ["project_name", "requirement_description", "business_rules"],
  "optional_fields": ["ui_mockups", "api_documentation", ...]
}
```
**作用**：
- 明确生成测试用例需要哪些输入信息
- 区分必填项和可选项
- 帮助用户准备充分的输入材料

---

### 4. 输出规范（Output）
```json
"output": {
  "format": "json",
  "schema": {...}
}
```
**作用**：
- 定义AI输出的格式（JSON/Markdown/XML）
- 指定JSON Schema结构，包含所有字段
- 确保输出可被程序解析和使用

**包含字段**：
- `id`: 用例唯一标识
- `title`: 用例标题
- `module/sub_module`: 所属模块
- `priority`: 优先级（P0-P3）
- `test_type`: 测试类型（positive/negative/boundary）
- `preconditions`: 前置条件数组
- `test_steps`: 测试步骤数组（包含步骤号、操作、预期结果、测试数据）
- `test_data`: 输入数据和期望输出
- `tags`: 标签数组
- `automation_level`: 自动化程度
- `remarks`: 备注

---

### 5. 测试方法（Methods）
包含6种经典测试方法：

1. **等价类划分法**
   - 将输入分为有效/无效等价类
   - 适用于：用户名、邮箱、手机号等字段

2. **边界值分析**
   - 测试边界值及附近值
   - 适用于：长度限制、范围限制、分页

3. **错误推测法**
   - 基于经验推测可能的错误
   - 适用于：SQL注入、XSS、特殊字符

4. **场景法**
   - 基于业务流程设计端到端场景
   - 适用于：注册流程、登录流程、密码找回

5. **状态迁移法**
   - 测试状态间的转换
   - 适用于：用户状态（未激活→激活→禁用）

6. **正交实验法**
   - 多因素组合测试
   - 适用于：多条件查询、权限矩阵

**作用**：指导AI使用系统化的测试方法生成用例

---

### 6. 领域规则（Domain Rules）
按业务领域划分：
- **用户注册**：唯一性验证、密码强度、验证码
- **用户登录**：多种登录方式、失败锁定、会话管理
- **个人信息**：头像上传、敏感信息保护
- **密码管理**：修改密码、重置密码、密码历史

每个领域包含：
- `must_test`: 必须测试的功能点
- `security_focus`: 安全关注点

**作用**：
- 确保关键业务规则不被遗漏
- 强调安全测试重点
- 提供测试覆盖率指导

---

### 7. 质量检查（Quality Checks）
10条质量检查项，例如：
- 每个功能点至少3个用例（正常、异常、边界）
- P0级用例必须覆盖核心功能
- 预期结果必须明确可验证
- 边界值必须精确定义
- 用例标题格式统一

**作用**：
- 确保生成用例的质量
- 提供验收标准
- 避免遗漏关键测试点

---

### 8. 提示词模板（Prompt Template）
这是最重要的部分！

**结构**：
1. **角色设定**：告诉AI它是谁、有什么能力
2. **测试任务**：明确要做什么
3. **输入信息**：需求描述、业务规则等
4. **测试方法指南**：详细说明每种测试方法
5. **领域规则**：各业务领域的必测项
6. **质量检查清单**：生成交付前的检查项
7. **输出要求**：格式、示例、注意事项

**使用变量**：
- `{{role.name}}`: 角色名称
- `{{role.expertise}}`: 专业知识列表
- `{{input.requirement_description}}`: 需求描述
- `{{methods}}`: 测试方法列表
- `{{domain_rules}}`: 领域规则
- `{{quality_checks}}`: 质量检查项

**作用**：
- 标准化AI的输入格式
- 确保每次生成质量一致
- 方便后续修改和优化

---

## 如何使用这个SKILL

### 1. 导入到系统
在SKILL管理页面点击"导入"，选择此JSON文件

### 2. 创建测试用例时
1. 选择此SKILL
2. 提供需求描述
3. 提供业务规则
4. AI会自动生成完整的测试用例集

### 3. 输出示例
AI会输出包含50+个测试用例的JSON，涵盖：
- 用户注册（15个用例）
- 用户登录（12个用例）
- 个人信息（10个用例）
- 密码管理（8个用例）
- 其他功能（5个用例）

每个用例包含完整的：标题、步骤、预期结果、测试数据

---

## 定制建议

你可以根据实际项目修改：
1. **name/code**: 改为你的项目名
2. **role.description**: 更具体的角色描述
3. **methods**: 添加/删除测试方法
4. **domain_rules**: 根据实际业务调整必测项
5. **quality_checks**: 根据团队规范调整
6. **prompt_template**: 优化提示词以获得更好结果
