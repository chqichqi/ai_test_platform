## 设备管理-疾病统计

# 站点地图 (基于探索数据)

## 模块路由表格

| 模块名称     | 路由路径 (推测) | 备注 |
|--------------|-----------------|------|
| 工作台       | (未提供)        | 导航项，具体路由未在探索数据中暴露 |
| 患者档案     | (未提供)        | 同上 |
| 设备管理     | (未提供)        | 同上 |
| 设备列表     | (未提供)        | 同上 |
| 设备收发     | (未提供)        | 同上 |
| 问卷管理     | (未提供)        | 同上 |
| 账号管理     | (未提供)        | 同上 |
| 数据管理     | (未提供)        | 同上 |
| 运营配置     | (未提供)        | 同上 |
| 操作日志     | (未提供)        | 同上 |
| 随访管理     | (未提供)        | 同上 |

> **说明**：探索数据仅包含导航项名称，`href` 均为空字符串，系统可能使用前端路由（Hash 或 History 模式），实际路径需在应用中进一步获取。起始 URL 为 `https://hospitalweb.xinjikang.cn/#/workpanel?refresh=...`，表明工作台路由可能为 `#/workpanel`，但其他模块路径未明确。

## 页面内跳转表格

| 跳转锚点 | 描述 | 所属页面 |
|----------|------|----------|
| (无)     | 探索数据未发现页面内锚点 | - |

## 注意事项

1. **数据来源限制**：本次探索仅基于提供的静态 JSON，未进行动态爬取，因此无法获取完整的路由映射。
2. **路由推断风险**：`start_url` 中的 hash 部分 `#/workpanel` 暗示前端路由模式，但禁止编造 URL，其他模块路径需官方确认或通过实际操作获取。
3. **查询参数**：起始 URL 包含 `refresh` 和 `oId` 参数，可能用于会话保持或防缓存，不反映路由结构。
4. **动态内容**：导航项可能包含子菜单或权限控制，探索数据未体现层级关系，使用时需注意。