# SKILL 快速复制功能说明

## 功能介绍

为了方便测试工程师快速复制预设SKILL模板，我们在操作列添加了两个复制按钮：

1. **普通复制**（图标按钮）：后端复制，生成副本
2. **快速复制**（带文字按钮）：前端复制，保留原SKILL所有内容，仅修改名称和编码

## 快速复制优势

### 对比

| 特性 | 普通复制 | 快速复制 |
|------|---------|---------|
| 操作位置 | 后端API复制 | 前端读取原SKILL详情后创建 |
| 复制内容 | 生成副本（编码加时间戳） | 完全保留原内容 |
| 自定义程度 | 低（自动生成编码） | 高（可自定义名称和编码） |
| 使用场景 | 简单复制 | 基于模板快速创建新SKILL |
| 步骤 | 1步点击 | 2步（点击+填写名称编码） |

### 快速复制流程

```
1. 点击【快速复制】按钮
   ↓
2. 弹出对话框，自动填充：
   - 名称：原名称 + "（副本）"
   - 编码：原编码 + "_copy"
   ↓
3. 用户修改名称和编码
   ↓
4. 点击【确定】
   ↓
5. 系统创建新SKILL，保留：
   - 角色定义
   - 测试方法
   - 提示词模板
   - 质量检查规则
   - 领域规则
   - 输入输出定义
   ↓
6. 新SKILL创建成功，立即可用
```

## 代码实现

### 后端复制（原有功能）

```typescript
const handleCopy = async (skill: Skill) => {
  await skillApi.copy(skill.id);  // 调用后端 /api/v1/skills/{id}/copy
  message.success('复制成功');
};
```

**后端行为**：
- 生成新编码：`{原编码}_copy_{时间戳}`
- 复制所有字段
- 标记为非默认

---

### 快速复制（新增功能）

```typescript
const handleQuickCopy = (skill: Skill) => {
  setCurrentSkill(skill);
  quickCopyForm.setFieldsValue({
    name: `${skill.name}（副本）`,
    code: `${skill.code}_copy`,
  });
  setQuickCopyModalVisible(true);
};

const handleQuickCopySubmit = async () => {
  const values = await quickCopyForm.validateFields();
  
  // 获取原SKILL详情
  const originalSkill = await skillApi.get(currentSkill.id);
  
  // 创建新SKILL，只修改名称和编码
  const newSkillData = {
    name: values.name,
    code: values.code,
    skill_type: originalSkill.skill_type,
    description: originalSkill.description,
    tags: originalSkill.tags,
    is_global: originalSkill.is_global,
    is_default: false,
    content: originalSkill.content,  // 保留完整内容
  };
  
  await skillApi.create(newSkillData);
  message.success('快速复制成功');
};
```

**前端行为**：
- 读取原SKILL完整详情
- 用户自定义名称和编码
- 保留所有content内容
- 创建新SKILL

---

## UI界面

### SKILL列表页

```
操作列：
┌─────┬─────┬─────────┬──────┐
│查看 │编辑 │ 复制图标 │ 复制 │ 删除 │
│ 👁️  │ ✏️  │   📋    │  ➕  │ 🗑️  │
└─────┴─────┴─────────┴──────┘
        ↑           ↑
   普通复制      快速复制（带文字）
```

### 快速复制弹窗

```
┌─────────────────────────────┐
│      快速复制SKILL           │
├─────────────────────────────┤
│ SKILL名称                    │
│ ┌─────────────────────────┐ │
│ │ 用户管理功能测试（副本） │ │
│ └─────────────────────────┘ │
│                             │
│ SKILL编码                    │
│ ┌─────────────────────────┐ │
│ │ user_management_copy    │ │
│ └─────────────────────────┘ │
│                             │
│ 快速复制将保留原SKILL的所有  │
│ 内容（角色定义、测试方法、提示 │
│ 词模板等），只需修改名称和编码 │
│ 即可使用。                   │
│                             │
│         [取消]  [确定]      │
└─────────────────────────────┘
```

---

## 使用建议

### 什么时候使用快速复制？

✅ **推荐使用快速复制的场景**：

1. **复制预设模板**
   - 复制功能测试模板创建业务特定SKILL
   - 复制WebUI模板创建页面特定SKILL
   - 复制API模板创建接口特定SKILL

2. **基于现有SKILL创建**
   - 同事创建的SKILL很适合当前项目
   - 需要微调但不重写
   - 保留测试方法，修改业务规则

3. **快速创建**
   - 时间紧迫，不想从头配置
   - 信任原SKILL的质量
   - 只需改个名字就能用

❌ **不推荐使用快速复制的场景**：

1. **需要大幅修改**
   - 测试方法完全不同
   - 提示词模板需要重写
   - 建议直接【创建SKILL】

2. **简单测试**
   - 只需要简单的测试用例
   - 建议直接【创建SKILL】走简单流程

---

## 示例

### 示例1：基于功能测试模板创建

**原SKILL**：功能测试用例生成专家（预设模板）

**快速复制**：
- 名称改为：用户管理功能测试专家
- 编码改为：user_management_test_v1

**结果**：新SKILL包含：
- 7种测试方法（等价类、边界值等）
- 完整的提示词模板
- 12条质量检查规则
- 只需提供需求即可生成测试用例

---

### 示例2：基于WebUI模板创建

**原SKILL**：WebUI自动化测试专家（预设模板）

**快速复制**：
- 名称改为：登录页面UI自动化
- 编码改为：login_page_ui_v1

**结果**：新SKILL包含：
- 8种UI测试方法
- Playwright定位策略
- 跨浏览器测试配置
- 只需提供页面URL即可生成自动化脚本

---

## 技术细节

### 弹窗组件

```typescript
<Modal
  title="快速复制SKILL"
  open={quickCopyModalVisible}
  onOk={handleQuickCopySubmit}
  onCancel={() => {
    setQuickCopyModalVisible(false);
    quickCopyForm.resetFields();
  }}
  confirmLoading={quickCopyLoading}
  width={500}
>
  <Form form={quickCopyForm} layout="vertical">
    <Form.Item
      name="name"
      label="SKILL名称"
      rules={[{ required: true }]}
    >
      <Input />
    </Form.Item>
    <Form.Item
      name="code"
      label="SKILL编码"
      rules={[{ required: true }]}
    >
      <Input />
    </Form.Item>
    <Form.Item>
      <Text type="secondary">
        快速复制将保留原SKILL的所有内容...
      </Text>
    </Form.Item>
  </Form>
</Modal>
```

### 表单自动填充

```typescript
// 点击快速复制时自动填充
quickCopyForm.setFieldsValue({
  name: `${skill.name}（副本）`,
  code: `${skill.code}_copy`,
});
```

---

## 总结

快速复制功能让测试工程师：

1. **零门槛使用预设模板**：不懂SKILL结构也能用
2. **一键复制**：2步完成SKILL创建
3. **保留最佳实践**：预设模板的质量检查规则
4. **自定义灵活**：可自由修改名称和编码
5. **立即生效**：复制后即可用于生成测试用例

**最佳实践**：
- 新手：快速复制预设模板 → 修改名称 → 使用
- 中级：快速复制 → 微调测试方法 → 使用
- 高级：快速复制 → 完全自定义 → 使用
