"""功能用例生成共享质量规则段（2026-09-03 审计 P1-2）。

主生成路径(version_generator/two_step_generator.build_step2_prompt)内联了这些规则；
本常量把**与输出格式无关的内容质量规则**（前置静态化/禁臆造数字/禁臆造URL/入口导航/
禁跨模块对象）抽为共享文本，供变更生成(requirement_change_service)、补偿生成
(test_case_auditor)、后台(async_generation_service)等一切功能用例生成路径注入，
满足方案 "生成侧规则必须 4 路径齐备" 契约——避免仅在主路径生效、其它路径"裸奔"
产出越界/臆造步骤（如把整条菜单或它模块按钮名写进「」）。

不含 JSON 结构/编号/条数约束——那些与各路径输出格式耦合，留在各自 prompt。
"""

GEN_QUALITY_RULES = """## 用例内容质量规则（必须逐条遵守）
1. 前置条件只写『执行本用例前必须已就绪的导航/入口状态』，且：指向真实可进入的页面级位置；
   页面内部的分类/分区/卡片区不是独立可进入页面，禁止写成『进入…XX分类页面』这类虚假导航词；
   禁止把『某卡片/某数据/某记录必然存在且正在展示』写成硬前提（出现与否由被测系统动态决定，
   应表述为『若出现则继续，未出现则跳过』）；禁止出现操作动词/动作指令（点击/添加/移除/保存/
   选择/输入/打开/后退等）；禁止在前置里写具体统计数/预置条数。
2. 禁止臆造需求未给出的具体数字。统计展示类（总数/今日新增/合计/条数）只能断言其结构
   （两项数值均展示且为有效数字/格式正确），不得编造『总数 N/今日新增 M』等凭空绝对值；
   仅当需求原文明确给出确切数字且确属断言目标时才允许原样引用。
3. 禁止臆造跳转目标 URL/路由。需求未给出明确目标页时，动作与期望一律写语义化目标
   （如『跳转至该卡片对应的详情/列表页面』），具体 URL 交由 UI 转化阶段真实探索解析绑定。
4. 页面入口导航必须是 test_steps 的第 1 步，禁止用『已处于/已进入 XX 页面』静态前置代替导航；
   需经导航到达的页面（文档开头『从…进入…』入口说明，或非登录默认落地页），该『从…进入…』
   动作必须作为本用例第 1 条步骤；只有登录即停在该页的默认落地页才允许不写入口导航。
5. 每条步骤的『操作对象/目标元素』必须来自本模块需求原文，只写本模块页面真实存在的对象名，
   严禁凭空引入本模块之外的菜单/页面名词（步骤对象跨模块会把探索带到错误页面）。
6. 每条步骤的「」只标记单个可操作对象名，不得把整条菜单/多个对象/换行枚举塞进一个「」。
"""

# 兼容别名（如被引用为主路径内联规则的补充），保持命名清晰
STEP2_QUALITY_RULES = GEN_QUALITY_RULES
